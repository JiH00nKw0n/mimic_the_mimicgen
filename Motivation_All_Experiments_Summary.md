# MimicGen 성공-필터 편향 — 2주 실험 총정리 (2026-07-17 ~ 07-31, red-team 검증판)

## 0. 왜 이 실험들을 하는가 — "다양성"은 축마다 다르게 작동한다

로봇 모방학습용 합성 데이터를 큐레이션할 때 통념은 "다양성은 선"이다. 그런데 MimicGen처럼 사람 데모 10개를 변형해 찍어내고 성공한 것만 남기는 생성기에서, 데이터의 다양성은 서로 성격이 다른 두 축으로 갈라진다.

**축 1 — 초기조건(변형 거리) 다양성.** 정책이 배치가 다른 상황들을 감당하려면 학습 데이터가 그 상태 공간을 덮어야 한다. 여기서 실질 질문은 "다양할수록 좋은가"가 아니라 **"얼마나 있으면 충분한가"** 다. 커버리지가 어느 선 아래로 떨어지면 무너지는가, 그 선 위에서는 분포 모양(밀도)이 문제가 되는가.

**축 2 — 소스(원본 데모) 다양성.** 합성 데모 하나하나는 원본 10개 중 하나의 궤적을 물려받는다. 성공 필터는 "잘 옮겨지는" 소수 원본 쪽으로 데이터를 쏠리게 하는데, 통념대로면 이 쏠림을 되돌려 소스를 고르게 해야 한다. 그러나 소스에는 품질 분포가 있다 — **다양성을 강제하는 것과 좋은 부분집합에 집중하는 것 중 무엇이 정책에 이로운가.**

이 두 축 위에서 우리가 답하려는 질문: **성공-필터가 만드는 쏠림 중 무엇이 정책에 해롭고, 무엇이 무해하며, 무엇이 오히려 이로운가 — 그리고 그것을 생성 로그(시도-생존 기록)만으로 감사할 수 있는가.**

2주간의 답을 미리 요약하면: 축 1의 쏠림은 검증한 범위에서 정책에 해가 없었고(밀도 재배분 무효), 축 2가 진짜 축이다(소스 품질 효과가 인과로 크게 확인). 다만 아래에 명시하듯 "확실한 것"과 "아직 가설인 것"의 경계가 있다.

**검증 절차 주석.** 이 문서는 초판 작성 후 다층 red-team(문서 주장 전수 원문 대조, 실험 방법론 4개 렌즈 감사, 통계 재계산)을 거쳐 재작성됐다. 초판의 paired McNemar p값들은 에피소드를 독립 표본으로 취급해 유의성을 부풀릴 수 있어(같은 학습 run 안의 에피소드는 상관됨), 본판은 **run-level(시드 단위) 재검정** 값을 함께 인용하고 그것을 판정 기준으로 삼는다.

용어: **DGR** = 생성 시도 중 성공해 남은 비율. **d_pos** = 합성 장면과 원본 데모의 물체 배치 거리(영역 크기로 정규화, 0~1).

---

## 실험 지도

| # | 실험 | 질문 | 태스크 | 층위 | 상태 |
|---|---|---|---|---|---|
| 1 | E1 생성 법칙 | 거리↑→DGR↓와 소스 쏠림이 태스크 일반인가 | 9 | 관찰(생성) | 완료 |
| 2 | E2 재균등화 | 쏠림을 학습 전에 되돌리면 정책이 좋아지나 | 8 | 정책(관찰) | 완료, 6시드 |
| 3 | exp2 대조 | 커버리지가 문제인가, 소스 품질이 문제인가 | 2~5 | 정책(관찰) | 완료 |
| 4 | 케이스 스터디 | 고/저DGR 소스의 합성 데모는 실제로 어떻게 다른가 | 8 | 정성 | 완료 |
| 5 | 통제 실험 | 위 결론들이 교란변수 없이 성립하나 (EXP-A/B/C2/C/D) | 2 | 인과 | 완료 |
| 6 | ctrl2 보강 | 인과 결론이 4~6개 태스크로 일반화되나 | 7 | 인과 | 진행 중 (78 run) |

공통 인프라: 태스크별 생성 풀(성공·실패 전량 로깅), BC-RNN-GMM 저차원 학습 2000 epoch(마지막 체크포인트 평가), 시드 고정 200개 초기상태(frozen resets)에서 전 조건 동일 장면 paired 평가.

---

## 실험 1 — E1: 생성 법칙 (9태스크)

**가설.** 이전 4태스크에서 본 두 관찰 — 변형 거리가 멀수록 DGR이 떨어지고, 성공만 남기면 소수 원본으로 쏠린다 — 은 우연이 아니다. 단, 공개 난이도 단계 일부는 영역 "넓히기"가 아니라 반대편 "옮기기"라 거리 축이 오염돼 있으므로 순수 확장형 변형으로 다시 재야 한다.

**셋업.** 9태스크. 옮기기형 공개 단계는 이전 영역을 감싸며 넓히는 확장 단계(D1E/D2E)로 재정의, 경계는 극단 위치 고정 프로브(위치당 50회)로 실측 동결. 변형당 500회 시도, 원본 선택 무작위·에피소드당 1개, 실패 전량 기록.

**결과.** 영역을 넓힌 변형일수록 풀 DGR이 9개 태스크 전부에서 하락(Stack 70→59%, Hammer 48→18%, Mug 31→13%). 살아남은 데모의 소스 쏠림은 확장과 함께 커지는 태스크가 많고(Coffee Prep 0.17→0.28, Mug 0.11→0.22), 극단 사례로 Mug 최광 단계는 원본 s7이 56회 시도에 생존 0개. 부수 확인: 공개 "옮기기" 단계는 거리 축 선외 구간을 차지하며, 위치만 뒤집은 Threading은 확장 추세 외삽과 대략 맞고(24.1% vs 22.0%) 방향까지 뒤집는 Coffee는 외삽보다 27.9%p 낮다.

**red-team 후 정정과 한계.**
- 쏠림 지표(TV 기반)는 생존 수가 적을수록 기계적으로 부풀려진다. 균등-생존 귀무가설 부트스트랩으로 보정하면 관측 쏠림의 30~50%가 소표본 효과다(예: Mug D2E 관측 0.217 중 귀무 기대 0.095). **방향은 살아남는다** — 최광 단계 다수가 귀무를 기각(p≤0.008)하고 s7 같은 전멸 사례는 노이즈로 설명 불가. 그러나 **크기와 태스크 서열, "저DGR일수록 심하다"는 스펙트럼의 정량 형태는 보정 전 수치라 과장돼 있다.**
- "거리 법칙"의 깨끗한 성립 단위는 **변형(영역) 수준**이다. 한 변형 풀 안에서의 거리-성공 관계는 태스크별로 들쭉날쭉하다(일부 풀은 양의 상관도 있음). "시도 하나하나의 거리가 성공을 결정한다"는 문장보다 "영역을 넓히면 풀 성공률이 떨어진다"가 데이터가 지지하는 형태다.
- 확장이 쏠림을 키우지 않는 반례도 있다(처음부터 DGR이 낮은 Stack Three·Three Piece는 넓혀도 제자리; 고DGR인 Stack·Coffee는 쏠림 자체가 거의 없음 0.04~0.07).
- 원본 선택을 무작위로 통일했으므로 이 수치들은 공개 MimicGen 파이프라인 그대로가 아니라 **통제된 변형판**의 성질이다(Square 재실행 DGR 21.0%는 논문 무작위-선택 조건 22.4%와 정합).

**결론.** 확장→DGR 하락(9/9)과 필터→소스 쏠림의 **방향**은 확증. 쏠림의 **크기**는 소표본 보정이 필요하고, "성공-필터면 어디서나"는 9개 태스크가 주는 귀납적 근거이지 증명이 아니다.

---

## 실험 2 — E2: 소박한 재균등화는 효과가 없다 (8태스크, 3-arm, 6시드)

**가설.** 필터가 만든 쏠림을 학습 전에 되돌리면 — 거리 구간 균등(transform_uniform) 또는 원본 균등(ancestry_balanced)으로 500개를 뽑으면 — 정책이 좋아진다, 특히 원본에서 먼 평가 상태에서.

**셋업.** 같은 풀에서 추출 규칙만 다른 3-arm(무작위 / 거리 5구간 각 100 / 원본별 균등), 8태스크 × 6시드(mug은 ancestry 불가), 동일 200장면 paired.

**결과 — transform_uniform: 무효.** 8태스크 풀링 9,600쌍에서 p=0.766. 데실·소스별·연속·효율 등 모든 컷에서 보정 생존 0개(적대적 6-검사 검증 포함). 유일하게 남는 신호 둘 다 약하다: hammer −2.2%p는 run-level에서 유의 소멸(t=−1.89, p≈0.12), three_piece +2.7%p는 6시드 중 5시드 방향 일관이지만 미유의(run-level p≈0.09) — 후보로만 남는다.

**결과 — ancestry_balanced: stack에서 실재하는 손해.** stack −7.5%p는 run-level로 재검정해도 살아남는다: **시드별 델타 [−7.5, −8.5, −7.0, −10.5, −9.5, −2.0]%p, 6/6 음수, t=−6.16, p≈0.002.** 서로 다른 시드 집합(2시드 코호트와 4시드 확장)이 같은 크기(−8.0 vs −7.2)로 재현했다. threading은 경계선(−3.3%p, run-level p≈0.07), 나머지 5태스크는 null. 2시드에서 유의했던 three_piece는 6시드에서 소멸(p=0.006→0.87). ancestry arm의 학습셋 거리 분포는 baseline과 동일(평균 d_pos 차 ≤0.005) — 손해는 거리가 아니라 소스 구성에서 온다.

**2시드→6시드가 말해주는 것.** 두 결과는 같은 실험이다(arm·풀·추출 규칙·200장면 동일, dataset seed 101–102를 101–106으로 순수 확장). 2시드의 transform 우세는 전부 소표본 착시로 판명: 최대였던 square near +7.5%p → +2.0, square 전체 +4.8 → +0.7, threading far는 부호까지 반전(−4.5 → +2.2). transform 쪽은 ±10%p에 이른 셀이 애초에 없었고, ±10%p급이었던 유일한 셀은 ancestry stack near −10.5%p — 그것만 −8.0으로 살아남았다. **시드를 늘렸을 때 사라진 것(transform 이득)과 남은 것(ancestry 손해)이 정확히 갈렸다.**

**결론.** 재균등화 가설 기각. 거리 균등화는 무효, 소스 균등화는 stack에서 유의하게 해롭고 나머지는 무효.

**한계.** 이 null은 **얕은 쏠림 용량에서의 null**이다 — 등방 재설계의 baseline 쏠림(TV 0.04~0.15)이 작아서 처치가 건드릴 것 자체가 적었다. 쏠림이 가장 큰 태스크들(coffee_prep 0.28, mug 0.22)은 풀이 얇아 정책 실험에서 빠졌다 — **편향이 심한 영역일수록 검증에서 빠지는 구조적 공백**이 있다. 저차원 관측 정책 한정.

---

## 실험 3 — exp2: 커버리지·소스 품질 관찰 대조

**가설.** (a) transform 무효는 "밀도는 무관, 커버리지에 문턱"으로 설명된다. (b) ancestry 손해는 저DGR 소스 데이터 유입 때문이다.

**셋업.** 같은 풀에서 극단 arm 추출(시드 201–203). near_only = 최근접 40% 거리에서 500개(square/coffee/stack/threading/three_piece 5태스크). hidgr_only/lodgr_only = 고DGR/저DGR 소스만으로 **350개**(stack/threading 2태스크). **주의: 이 실험은 baseline(시드 101–106)과 시드가 달라 짝지음이 안 되고, hidgr/lodgr는 크기도 baseline(500)과 다르다 — 아래 수치는 통계 검정 없는 관찰값이다.**

**결과.** 거리 3등분 성공률(baseline 대비):

| task | near_only near/mid/far | 해석 |
|---|---|---|
| square | −0.3 / **−12.7** / **−14.2** | mid·far 붕괴 |
| coffee | +1.5 / −2.3 / **−13.9** | far 한정 붕괴 |
| threading | +2.3 / **−12.2** / −6.8 | mid·far 붕괴 |
| three_piece | +7.0 / −0.8 / **−8.1** | far 붕괴 (바닥 SR 0.19라 잡음 큼) |
| stack | −4.0 / −4.7 / −3.7 | 완만 — 붕괴라 하기 애매 |

hidgr vs lodgr(같은 크기 350끼리의 깨끗한 비교): stack 0.948 vs 0.440(+50.8%p), threading 0.688 vs 0.420(+26.8%p). hidgr는 350개로 baseline 500개보다도 높다(stack +11, threading +17%p) — 단 이건 크기·거리 분포가 다른 비짝지음 비교라 방향 참고용이다.

**결론.** 커버리지 상실은 잃은 구간에서 성능을 깎고(부피 500 동일하므로 밀도 아닌 커버리지), 소스 품질 격차는 그보다 훨씬 크다 — 둘 다 **관찰 수준**. 인과 검정은 실험 5로.

---

## 실험 4 — 케이스 스터디 (8태스크, 정성)

태스크마다 DGR 최고/최저 소스(성공 데모 3개 이상 중)를 골라 실제 합성 데모 영상을 나란히 놓았다(태스크당 6클립 — 고/저 소스 × {성공-근거리, 성공-원거리, 실패-원거리}, 총 48클립). 고DGR 소스는 거리와 무관하게 깔끔히 성공하고, 저DGR 소스는 실패가 잦고 성공도 아슬아슬하다. 정량 결론에는 안 쓰고, 실험 5의 설계 동기(저품질 소스의 "성공" 데이터도 나쁠 수 있다)를 준 관찰이다.

---

## 실험 5 — 통제 실험: 한 번에 한 축만 (stack·threading)

관찰 결론의 교란 반론(특히 "transform 균등화 때 나쁜 데모가 섞인 것 아니냐")을 닫기 위해, 소스·품질·크기·거리 분포를 얼려 놓고 한 축씩 흔들었다. 평가는 동일 frozen-reset paired, 판정은 run-level.

**EXP-A — 밀도 (stack, 6시드).** 같은 소스 {4,7}, 같은 500개, near:far만 3:1 vs 1:3. 전체 −0.6%p, run-level 등가검정(TOST ±5%p) 통과 p=0.007. "나쁜 데모 혼입" 반론은 여기서 닫힌다 — 소스·품질이 비트 단위로 같다. **단서:** 가장 먼 평가 구간(bin4)은 CI가 [−7.0,+1.9]%p로 ±5%p를 벗어나 그 구간까지 등가라고는 말할 수 없고, 이 등가는 고DGR 소스·천장(0.96) 태스크 1개에서의 결과다.

**EXP-B — 커버리지 (stack, 3시드).** 같은 소스, 같은 400개, far 데모 유무만 차이. 전체 0.955 vs 0.955, far 평가에서도 유의차 없음. **단서(중요):** far 슬라이스는 쌍이 111/78개뿐이라 탐지 하한이 9~14%p다 — exp2에서 본 붕괴 크기(8~14%p)가 통째로 탐지 불가 영역에 들어간다. 즉 이 결과는 "붕괴를 배제했다"가 아니라 "큰 붕괴는 없었다"까지다. 게다가 B arm은 고DGR 소스만 썼고(잘 일반화하는 소스), far 평가 상태의 ~15%는 학습 지지 밖이다.

**EXP-C2 / C — 소스 품질 (stack / threading, 3시드).** 거리 5분위 완전 매칭, 소스 쌍만 교체. **stack {4,7} vs {3,1}: +19.8%p — 시드별 [+20.0, +21.0, +18.5]%p, 겹침 전무, run-level p=0.001. 이 프로그램에서 가장 단단한 인과 결과다.** 저품질 쌍은 자기 소스에서 멀수록 더 나빠진다(near 0.88 → far 0.61). threading의 3단계 비교(hi 0.635 / mid 0.535 / lo 0.530)는 pair-level로는 hi 우세가 유의했지만 **run-level에서는 시드별 델타가 [+19.5, +0.5, +10.0]로 널뛰어 p=0.21 — "상위 프리미엄" 서열 주장은 3시드로 못 세운다.**

**EXP-D — DGR 충분통계인가 (threading, 3시드).** 평균 DGR을 맞춘 두 소스쌍({2,0} 0.58 vs {8,1} 0.61), 거리 매칭. D_1이 +9.2%p — run-level p=0.039로 살아남는 **존재 증명**: DGR이 같아도 정책 가치가 다른 소스 구성이 존재한다. **단서:** "소스 1이 범인"이라는 세부 소급(C_hi vs D_2 +13%p)은 run-level p=0.13으로 미확정이고, 이 반례가 소스 속성인지 소스 1 안의 소수 불량 데모 탓인지(그렇다면 per-source가 아니라 per-demo 문제) 이 설계로는 못 가른다. 1태스크·소스쌍 1개.

---

## 실험 6 — ctrl2: 인과 결론의 태스크 일반화 (진행 중)

실험 5의 약점(EXP별 1~2태스크)을 메우기 위해 8개 풀 전체에 feasibility 게이트를 돌리고(7개 통과, mug 탈락) 78개 학습을 런칭했다(07-31, 시드 301–303): 밀도(A)와 커버리지(B)를 threading·square·coffee로 — **B는 near_only가 실제 붕괴했던 태스크들에서의 재검정이라 문턱 가설의 운명을 가른다** — 품질(C2)을 square·coffee·three_piece·stack_three로, DGR 매칭(D)을 stack·square·coffee로 확장. 학습 약 23시간 + 평가 5시간, 08-01 오후 완료 예상.

---

## 확실히 말할 수 있는 것 (run-level 검정과 다층 검증을 통과한 것만)

1. **영역을 넓히면 생성 성공률이 떨어지고(9/9 태스크), 성공 필터는 소스 쏠림을 만든다(방향).** 극단적으로는 특정 원본이 데이터에서 통째로 사라진다(mug s7: 56시도 0생존).
2. **살아남은 데이터의 거리 쏠림을 소박하게 재균등화하는 것은, 검증한 얕은-쏠림 영역에서, 정책에 아무 효과가 없다.** 8태스크 9,600쌍 풀링 null + 전수 슬라이싱 보정 생존 0 + 밀도 인과 등가(EXP-A, run-level TOST p=0.007). 2시드에서 보였던 이득은 시드를 늘리자 전부 사라진 소표본 착시였다.
3. **소스 균등화(ancestry)는 stack에서 실재하는 손해를 낸다.** run-level 6/6 음수, t=−6.16, p≈0.002, 독립된 두 시드 코호트에서 같은 크기로 재현. (나머지 태스크에서는 무효 — "해롭다"의 일반화가 아니라 "이득이 없고 위험할 수 있다"가 정확한 형태다.)
4. **소스 구성은 거리를 완전히 매칭해도 정책 성능을 크게 바꾼다 — 인과.** stack C2 +19.8%p, 시드 겹침 전무, run-level p=0.001.
5. **DGR이 거의 같아도 정책 가치가 다른 소스 구성이 존재한다** (EXP-D, run-level p=0.039 존재 증명). DGR만으로 소스 가치를 요약할 수 없다는 하한선.

## 아직 가설인 것 (미완성 — 다음 검증 대상)

1. **커버리지 문턱 "최근접 40~60% 사이".** 구간의 양 끝이 서로 다른 태스크에서 왔고(붕괴는 square/coffee에서, 무붕괴는 stack에서), stack은 40%에서도 애초에 안 무너졌으며, 무붕괴 쪽 검정력은 붕괴 크기를 탐지 못 하는 수준이다. → ctrl2-B(square/coffee)가 직접 검정 중.
2. **"필터의 고DGR 편중은 이로운 편향"이며 큐레이션은 좋은 소스로 집중해야 한다.** 유일한 직접 증거(hidgr>baseline)는 비짝지음·크기 불일치(350 vs 500)·거리 교란이 열린 관찰이다. "편중 vs 탈편중 vs baseline"을 정면으로 겨루는 인과 arm은 아직 없다.
3. **소스 품질 효과의 태스크 일반성.** 인과 확인은 stack(+C 방향 지지)뿐이고, stack 효과조차 "쌍 전체의 품질"인지 "특정 독성 소스 하나"인지 안 갈렸다. → ctrl2-C2(4태스크 추가).
4. **threading의 "품질 3단계 서열(hi>mid≈lo)".** run-level p=0.21. 시드 확장 없이는 주장 불가.
5. **"저DGR 태스크일수록 쏠림이 심하다"는 스펙트럼의 정량 형태.** 관측 쏠림의 30~50%가 생존 수 소표본 효과라 보정 후 재작성이 필요하다.
6. **"DGR 프로파일로 걱정 태스크를 선별하고, 걱정되면 균등화 금지"라는 실무 규칙.** 증거 패턴이 규칙과 어긋난다 — 균등화 해악이 실증된 유일한 태스크(stack)는 규칙이 "안심"으로 분류하는 고DGR·저쏠림 태스크다. 사슬 전체가 한 태스크에서 시연된 적이 없다.
7. **고쏠림 영역(방향성 D2, mug, coffee_prep급)에서도 재균등화가 무효인가.** 미검정 — 지금까지의 null은 전부 얕은 쏠림 용량에서 나왔다.

## 방법론 한계 (red-team이 확인한 것)

- **검정 단위.** 초판의 McNemar p값들은 에피소드 단위 풀링이라 시드 군집을 무시했다. 본판의 판정은 run-level 재검정 기준이며, 이 기준에서 hammer의 "transform 유의 손해"(p=0.0073)는 소멸했다(p≈0.12).
- **평가셋 재사용.** 태스크당 200개 frozen reset 한 벌이 모든 실험·모든 사후 분석에 재사용됐다(가설 생성과 확인이 같은 장면 위에서 이뤄짐). 새 arm의 새 학습이 부분적 독립성을 주지만, 장면 수준 일반화는 미검증이다.
- **프로토콜.** 마지막 체크포인트 평가(논문은 best-of-training) — arm 간 비교는 공정하나 절대 SR은 논문과 다르다. 원본 선택 무작위 통일 — 공개 파이프라인 그대로의 수치가 아니다.
- **범위.** 저차원 관측·BC-RNN·단일 시뮬레이터. 고쏠림 태스크들이 정책 층에서 구조적으로 빠졌다.

## 재현 경로

- E1: `Motivation_Extended_Tasks.md` / E2: `Motivation_E2_Policy_Results.md`(부록 A~D) / 통제실험: `Motivation_Controlled_Results.md` + `motivation/data/ctrl_eval/` + `motivation/scripts/ctrl_analyze.py`
- ctrl2: `motivation/scripts/ctrl2_{gate,build_arms,run}.{py,sh}` (서버 aidas-l40s, 로그 `~/ctrl2_run.log`)
- run-level 재검정 값: 시드별 SR은 각 태스크 `e2_arms/<task>_N2/eval/*.jsonl`에서 계산 (본문 수치 그대로 재현 가능)
