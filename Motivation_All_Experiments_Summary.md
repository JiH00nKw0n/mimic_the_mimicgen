# MimicGen 성공-필터 편향 — 2주 실험 총정리 (2026-07-17 ~ 07-31)

MimicGen은 사람 데모 10개를 새 물체 배치로 변형(transform)해 합성 데모를 만들고, 시뮬레이터에서 성공한 것만 남긴다(retain). 우리가 의심한 것은 이 마지막 필터다: 성공한 것만 남기면 데이터가 특정 방향으로 쏠리고, 그 쏠림이 정책 성능을 해칠 수 있다. 지난 2주 동안 이 의심을 관찰 → 정책 효과 → 인과 통제의 세 층으로 검증했다. 결론을 먼저 말하면 — 쏠림은 실재하지만 통념과 반대로 작동한다. 거리·밀도·커버리지 축의 쏠림은 정책에 거의 무해하고, 진짜 축은 어느 원본 데모(source)가 데이터를 만들었느냐다. 심지어 필터가 만드는 고성공 소스 편중은 해롭기는커녕 이로운 쪽이다.

용어 두 개만 미리 정의한다. **DGR**(data generation rate) = 생성 시도 중 성공해 데이터셋에 남은 비율. **변형 거리(d_pos)** = 합성 장면의 물체 배치가 원본 데모의 배치에서 얼마나 떨어졌는지를 영역 크기로 정규화한 값(0~1).

## 실험 지도

| # | 실험 | 질문 | 태스크 | 층위 | 상태 |
|---|---|---|---|---|---|
| 1 | E1 생성 법칙 | 거리↑→DGR↓와 소스 쏠림이 일반 법칙인가 | 9 | 관찰(생성) | 완료 |
| 2 | E2 재균등화 | 쏠림을 학습 전에 되돌리면 정책이 좋아지나 | 8 | 정책(관찰) | 완료, 6시드 |
| 3 | exp2 대조 | 커버리지가 문제인가, 소스 품질이 문제인가 | 2~4 | 정책(관찰) | 완료 |
| 4 | 케이스 스터디 | 고/저DGR 소스의 합성 데모는 실제로 어떻게 다른가 | 8 | 정성 | 완료 |
| 5 | 통제 실험 | 위 결론들이 교란변수 없이 성립하나 (EXP-A/B/C2/C/D) | 2 | 인과 | 완료 |
| 6 | ctrl2 보강 | 인과 결론이 4~6개 태스크로 일반화되나 | 7 | 인과 | 진행 중 (78 run) |

공통 인프라: 태스크별 생성 풀(가장 넓은 변형 N2에서 6,250회 시도, 성공·실패 전량 로깅), BC-RNN-GMM 저차원 학습 2000 epoch, 시드 고정 200개 초기상태(frozen resets)에서 전 조건 동일 장면 paired 평가, 에피소드 단위 McNemar 정확검정.

---

## 실험 1 — E1: 생성 법칙은 태스크 일반이다 (9태스크)

**가설.** 이전 4태스크에서 본 두 관찰 — (1) 변형 거리가 멀수록 DGR이 떨어진다, (2) 성공만 남기면 살아남은 데모가 소수 원본으로 쏠린다 — 은 우연이 아니라 성공-필터 생성의 일반 성질이다. 단, 공개 난이도 단계 일부는 영역을 "넓히는" 게 아니라 반대편으로 "옮기는" 것이라 거리 축이 오염돼 있으므로, 순수 확장형 변형으로 다시 재야 한다.

**셋업.** 태스크 9개(Square, Threading, Coffee, Three Piece Assembly, Stack, Stack Three, Mug Cleanup, Hammer Cleanup, Coffee Preparation). 옮기기형 공개 단계는 이전 영역을 감싸며 넓히는 확장 단계(D1E/D2E)로 새로 정의했고, 경계는 감이 아니라 실측으로 동결했다(극단 위치 고정 50회 프로브에서 도달률이 안쪽과 동급일 때만 통과, 걸린 두 곳은 2cm 축소). 변형당 시도 500회, 원본 선택은 무작위·에피소드당 1개로 통일, 실패도 전량 기록.

**결과.** 9개 태스크 전부에서 거리-DGR 곡선이 예외 없이 단조 하락한다(Stack 70→59%, Hammer Cleanup 48→18%, Mug Cleanup 31→13%). 소스 쏠림(생존 분포와 균등 분포의 TV 거리에서 시도 단계 몫을 뺀 값)은 영역을 넓힐수록 커지고(Coffee Prep 0.17→0.28, Mug 0.11→0.22, Hammer 0.00→0.15), DGR이 낮은 태스크일수록 심하다. 극단 사례로 Mug Cleanup 최광 단계에서는 원본 s7이 56회 시도에 생존 0개 — 실질 원본 수가 8.5개에서 6.9개로 준다. 부수 확인: 공개 "옮기기" 단계는 거리 축의 선외 구간을 차지하며, 위치만 뒤집은 Threading은 확장 추세의 외삽과 일치하지만(예측 24.1% vs 실측 22.0%) 방향까지 뒤집는 Coffee는 외삽보다 27.9%p 더 낮다 — 거리가 난이도의 1차 설명이고 반전은 별도 난이도다.

**결론.** 두 관찰 모두 확증. 성공-필터 생성이면 어디서나 나타나고, 걱정해야 할 태스크(저DGR)를 미리 가늠할 수 있다.

---

## 실험 2 — E2: 쏠림을 소박하게 되돌리는 건 효과가 없다 (8태스크, 3-arm, 6시드)

**가설.** 필터가 만든 쏠림을 학습 전에 되돌리면 — 변형 거리 구간별로 균등하게 뽑거나(transform_uniform), 원본별로 균등하게 뽑으면(ancestry_balanced) — 정책이 좋아진다. 특히 원본에서 먼 평가 상태에서.

**셋업.** 같은 생성 풀에서 500개를 뽑되 규칙만 셋: 무작위(baseline) / 거리 5구간 각 100개 / 원본별 균등. 생성·시뮬레이터·원본이 전부 동일하므로 차이는 추출 규칙뿐. 8태스크 × 3arm × 6시드(mug은 생존 풀이 얇아 ancestry arm 제외), 동일 200장면 paired 평가(태스크당 1,200쌍).

**결과 — transform_uniform: 완전한 무효.** 전 태스크 ±1~3%p 안에서 무의미하고, 8태스크를 풀링한 9,600쌍의 최대검정력 앵커에서 p=0.766. 거리 데실·소스별·연속·효율(스텝 수)·시드 방향 등 가능한 모든 컷으로 쪼개도 Bonferroni 생존 0개였고, 6개 검사-에이전트가 차이 후보를 적대적으로 끌어내고 판정자가 거른 13-에이전트 검증에서도 진짜 차이 판정 0개. 유일한 유의는 hammer에서 오히려 −2.2%p(p=0.0073) — 반대 방향 경계 신호다. 참고로 2시드 시점엔 transform이 5/8 태스크에서 앞서 보였는데(square +4.8%p) 6시드에서 전부 0으로 수렴했다 — 소표본 착시였다.

**결과 — ancestry_balanced: 무효 아니면 해로움.** 유일하게 유의한 효과는 stack의 −7.5%p(p<0.001, near/mid/far 전 구간 유의)다. 2시드에서 유의했던 three_piece는 6시드에서 소멸했다(p=0.006→0.87). 그리고 결정적 단서: ancestry arm의 학습셋 거리 분포는 baseline과 사실상 동일하다(평균 d_pos 차 ≤0.005) — 즉 stack의 손해는 거리 재배분이 아니라 순수하게 어떤 원본을 얼마나 담았느냐에서 온다.

**결론.** 가설 기각. 편향은 실재하는데(E1) 뻔한 처방은 듣지 않고, 소스 축을 건드리면 오히려 위험하다. 이 시점에서 두 의문이 남았다 — transform이 정말 아무 역할도 없나(극단적으로 줄이면?), ancestry는 왜 해로운가(소스에 뭐가 있나).

---

## 실험 3 — exp2: 커버리지와 소스 품질, 두 갈래 관찰 대조

**가설.** (a) transform 축은 "밀도는 무관하지만 커버리지에는 문턱이 있다"로 설명된다 — 균등화가 무효인 건 baseline도 이미 문턱 위이기 때문. (b) ancestry가 해로운 건 균등화가 저DGR 소스의 데이터를 끌어올리기 때문이다.

**셋업.** 같은 풀에서 극단 arm을 추가로 추출(시드 201–203, baseline과 시드가 달라 장면 단위 비교). near_only = 최근접 40% 거리에서만 500개(far 커버리지 제거; square/coffee/stack/threading). hidgr_only / lodgr_only = 고DGR 소스만 / 저DGR 소스만으로 500개(stack/threading).

**결과.** 거리 3등분(near/mid/far) 성공률:

| task | baseline (n/m/f) | near_only (n/m/f) | hidgr_only | lodgr_only |
|---|---|---|---|---|
| square | .749/.714/.566 | .746/**.587**/**.424** | — | — |
| coffee | .672/.585/.422 | .687/.562/**.283** | — | — |
| stack | .871/.848/.795 | .831/.801/.758 | **.948** 전체 | **.440** 전체 |
| threading | .619/.485/.447 | .642/**.363**/.379 | **.688** 전체 | **.420** 전체 |

near_only는 near에서 baseline과 같지만 mid/far에서 −7~−14%p 무너진다(stack만 완만, −4%p). 학습 부피는 똑같이 500개이므로 밀도가 아니라 커버리지의 문제다. 소스 품질 쪽은 격차가 훨씬 크다: hidgr−lodgr가 stack +50.8%p, threading +26.8%p이고, hidgr는 baseline조차 +11/+17%p 이긴다.

**결론.** 두 가설 모두 관찰 수준에서 지지. 다만 이 arm들은 거리 분포가 통제되지 않아(hidgr 소스는 far 생존도 많다) 인과 주장은 아직 못 한다 — 그래서 실험 5로 간다.

---

## 실험 4 — 케이스 스터디: 실제 영상으로 본 소스 품질 (8태스크, 정성)

각 태스크에서 DGR 최고 소스와 최저 소스(성공 데모 3개 이상인 것 중)를 골라, 성공·실패 × 가까운/먼 변형의 실제 합성 데모를 영상으로 나란히 놓았다(45클립 갤러리). 고DGR 소스는 거리와 무관하게 깔끔히 성공하고, 저DGR 소스는 대부분 실패하며 드문 성공도 아슬아슬하다 — 저DGR 소스의 "성공" 데이터조차 학습에 좋은 데이터가 아닐 수 있다는 직관을 준다. 이 직관은 실험 5의 EXP-C2(저품질 소스 데이터는 멀수록 더 나쁨)와 EXP-D(DGR만으로는 부족)로 정량 확인된다.

---

## 실험 5 — 통제 실험: 축을 하나씩 얼리고 흔들었다 (stack·threading)

관찰 결론에는 교란 반론이 남는다 — 특히 "transform을 균등하게 뽑다 보니 나쁜 데모가 섞여서 이득이 상쇄된 것 아니냐". 그래서 소스 정체성·품질·크기·거리 분포를 각각 얼려 놓고 한 번에 한 축만 흔드는 arm들을 설계했다(상세: Motivation_Controlled_Results.md). 평가·통계는 실험 2와 동일한 frozen-reset paired McNemar.

**EXP-A — 밀도 (stack, 6시드).** 가설: 거리 밀도 재배분은 효과가 없다(있다면 far 데이터가 far 평가를 돕는다). 셋업: 같은 소스 {4,7}, 같은 500개, near:far 비율만 3:1 vs 1:3. 결과: 차이 −0.6%p, p=0.39, 90% CI [−1.5, +0.4]%p — 등가 기준 ±5%p TOST 통과. 가장 먼 평가 구간에서도 무차이. 결론: 밀도 무효 확정. "나쁜 데모 혼입" 반론은 여기서 죽는다 — 이 비교엔 소스도 품질도 완전히 같다.

**EXP-B — 커버리지 (stack, 3시드).** 가설: near_only 붕괴는 부피가 아니라 far 커버리지 상실 때문이다. 셋업: 같은 소스, 같은 400개, far 구간 데모 유무만 차이(B_far 150/100/150 vs B_nearpad 300/100/0). 결과: 전체 0.955 vs 0.955, far 평가에서도 무붕괴. 결론: 예상 밖 — far 커버리지 자체는 불필요했다. 실험 3의 near_only(최근접 40%, mid도 없음)는 무너졌으므로, 커버리지 문턱은 40~60% 사이 어딘가이고 그 너머는 일반화가 메운다. 필터의 near 편중이 무해한 이유다.

**EXP-C2 / EXP-C — 소스 품질 (stack / threading, 3시드).** 가설: 소스 품질 효과는 거리를 완전히 매칭해도 남는다(인과). 셋업: 거리 5분위마다 같은 수를 뽑아 두 arm의 거리 분포를 동일하게 만들고 소스 쌍만 다르게 — stack {4,7}(DGR 0.94) vs {3,1}(0.69), threading은 hi{2,8}/mid{0,6}/lo{4,5} 3단계. 결과: stack +19.8%p(0.963 vs 0.765, p=1.9e-26), threading hi가 mid/lo를 +10%p(p≈1e-4)로 이기되 mid vs lo는 무차이. 저품질 소스 데이터는 자기 소스에서 멀수록 더 나빠진다(C2_mid: near 0.88 → far 0.61). 결론: 소스 품질이 진짜 축임을 인과로 확정. 효과는 선형이 아니라 상위 소스 프리미엄 형태다.

**EXP-D — DGR은 충분통계인가 (threading, 3시드).** 가설: 소스의 정책 가치가 DGR로 요약된다면, 평균 DGR을 맞춘 두 소스쌍은 동률이어야 한다. 셋업: D_1{2,0}(평균 DGR 0.58) vs D_2{8,1}(0.61), 거리 분포 매칭. 결과: D_1이 +9.2%p(p=8.4e-4) — DGR이 높은 쪽이 진다. 교차 비교로 범인을 좁히면 소스 8→0 교체는 무해(+3.8, p=0.14)한데 소스 2→1 교체가 −13.0%p(p=1.2e-6): threading 소스 1은 DGR 0.60으로 상위권인데 정책 학습에는 해로운 데이터를 만든다. 결론: DGR은 좋은 1차 프록시지만 충분통계가 아니다. "생성이 잘 되는 소스"와 "학습에 좋은 소스"는 겹치지만 같지 않다.

---

## 실험 6 — ctrl2: 인과 결론의 태스크 일반화 (진행 중)

실험 5의 약점은 태스크 폭이다(EXP별 1~2태스크). 그래서 8개 풀 전체에 feasibility 게이트(소스별 DGR 격차·거리 bin 점유)를 돌려 7개 태스크에서 보강이 가능함을 확인하고(mug_cleanup만 풀 부족), 다음을 런칭했다(2026-07-31, 78개 신규 학습, 시드 301–303):

| EXP | 추가 태스크 | 최종 커버 |
|---|---|---|
| A 밀도 | threading, square, coffee | 4태스크 |
| B 커버리지 | threading, square, coffee (near_only가 무너졌던 그 태스크들) | 4태스크 |
| C2 품질 | square, coffee, three_piece, stack_three | 6태스크 |
| D DGR충분성 | stack, square, coffee | 4태스크 |

생성 풀·평가 인프라를 그대로 재사용하므로 새 생성은 없다. 학습 약 23시간 + 평가 약 5시간 예상.

---

## 최종 결론

1. **필터 편향은 실재하고 일반적이다** (E1, 9/9 태스크). 거리가 멀수록 덜 남고, 남는 것은 소수 원본으로 쏠리며, 쏠림은 DGR이 낮은 태스크일수록 심하다.
2. **그러나 거리 축의 쏠림은 정책에 거의 무해하다** (E2 null → EXP-A/B 인과 확정). 밀도 재배분은 완전 통제 하에서도 등가이고, far 커버리지조차 통째로 빼도 무너지지 않는다(문턱은 최근접 40~60% 사이). 성공-필터가 만드는 near 편중은 정책 관점에서 걱정거리가 아니다.
3. **진짜 축은 소스 품질이다** (exp2 관찰 → EXP-C2/C 인과 확정). 거리를 완전히 매칭해도 소스 쌍 교체만으로 stack +20%p, threading +10%p. ancestry 균등화가 해로운 메커니즘도 이것이다 — 저품질 소스의 데이터를 정책에 다시 밀어 넣는 것.
4. **따라서 필터의 고DGR 소스 편중은 사실상 이로운 편향이다.** hidgr_only가 baseline을 이기는 것(+11~17%p)이 그 증거다. 큐레이션이 개입할 곳은 거리 축이 아니라 소스 축이고, 방향은 "균등화"가 아니라 "좋은 소스로의 집중"이다.
5. **다만 DGR은 소스 가치의 불완전한 프록시다** (EXP-D). DGR을 맞춘 쌍 사이에 +9%p가 남고, DGR 상위권이면서 정책에 해로운 소스가 실존한다.

**시사점.** (a) 우리 감사 프레임워크(attempt-vs-survivor 로그 기반)의 다음 지표는 per-source training value — 생성 로그만으로 "이 소스가 만든 데이터가 정책에 얼마나 기여하는가"를 추정하는 것이 목표이고, EXP-D가 그 필요성의 직접 증거다. (b) 실무적으로는, 태스크의 DGR 프로파일만 봐도 소스 편향을 걱정해야 할지(저DGR 태스크) 가늠할 수 있고, 걱정된다면 소스별 균등화는 금물이다. (c) "합성 데이터의 다양성을 되살리자"는 통념적 큐레이션 서사는 적어도 이 축들에서는 데이터가 반박한다.

## 한계와 다음 단계

- 인과 층의 태스크 폭이 아직 1~2개다 — ctrl2(7태스크)가 돌고 있고, 완료되면 이 문서의 결론 2·3·5를 다태스크로 재검정한다.
- 저차원 관측 정책 한정이다. 이미지 정책에서의 재현은 미착수.
- stack은 천장(0.96) 근처라 커버리지·밀도 효과가 압축돼 보일 수 있다. ctrl2의 중간 SR 태스크들이 이를 보완한다.
- 소스 1(threading) 같은 "고DGR·저가치" 소스가 왜 생기는지(궤적 스타일? 특정 구간의 아슬아슬함?)는 케이스 스터디 수준이다. per-source value 지표 설계와 함께 규명할 것.

## 재현 경로

- E1: `Motivation_Extended_Tasks.md` — 풀·게이트·그림 전부.
- E2(+전수 슬라이싱): `Motivation_E2_Policy_Results.md` 부록 A~D.
- 통제 실험: `Motivation_Controlled_Results.md`, per-episode 데이터 `motivation/data/ctrl_eval/`, 분석 `motivation/scripts/ctrl_analyze.py`.
- ctrl2: 게이트 `motivation/scripts/ctrl2_gate.py`, arm 구성 `ctrl2_build_arms.py`, 오케스트레이터 `ctrl2_run.sh` (서버 aidas-l40s, 로그 `~/ctrl2_run.log`).
