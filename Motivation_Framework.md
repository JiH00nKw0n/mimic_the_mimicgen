# 부하-예산 프레임워크: 합성이 왜 실패하는지를 관측 이전 수준에서 설명하기

2026-08-05. 9-에이전트 심층 조사(MimicGen 계열·모방학습 데이터 이론 문헌 재독, 생성기 코드 제1원리 유도, 보유 로그 검증, red-team 3종)의 판정을 정리한 프레임워크 제안. 판정 원문: `motivation/data/framework_judge.json`.

## 0. 무엇이 문제였나

지금까지의 결과(생성 성공률 하락, 원본 쏠림, 정책 절벽 등)는 전부 **관측**이다. 원본별 생존 곡선 같은 요약도 여전히 성능 측정이지 설명이 아니다. 필요한 것은 한 층 아래 — **원본 데모 자체의 성질, 변형 방법의 성질, 변형 크기의 성분**이 어떻게 그 관측들을 만들어내는지에 대한 인과 구조다. 그래서 이 프레임워크의 모든 양은 하나의 규율을 지킨다:

> **비순환 규율.** 프레임워크의 양은 원본 hdf5 + 변형 명세 + 작업 기하만으로, 시뮬레이터를 한 스텝도 돌리지 않고 계산 가능해야 한다. 생성 성공률과 로봇 성공률은 오직 예측 대상으로만 등장한다.

이것이 가능한 근거가 이 프레임워크의 핵심 발견이다: **MimicGen이 실행할 계획 궤적은 (원본, 변형, 배치)의 결정론적 닫힌형 함수다.** 생성기가 궤적을 "시도"하기 전에 그 궤적 전체를 종이 위에서 계산할 수 있고, 따라서 시도 풀 전체를 사전에 분석할 수 있다.

## 1. 핵심 주장

모든 생성 시도는 **부하(load)** 와 **예산(budget)** 의 대결이다.

- **부하** = 변형이 그 시도에 가하는 스트레스. 스칼라 거리가 아니라 **성분별 벡터**다: 집는 물체의 이동, 받는 물체의 이동, (지렛대 증폭된) 회전, 구간 사이를 잇는 다리(보간) 길이, 작업공간 상의 절대 위치, 주변 여유 공간. **← 다양성 계열 원인** (어떤 배치들을 시도하는가가 결정).
- **예산** = 원본 데모가 갖고 태어난 여유. 속도 여유(스텝당 0.05m 행동 한계 대비 얼마나 여유 있게 움직였나), 접촉 깔때기 잔여 마진(작업 공차 중 데모 자신의 실행이 이미 소진한 몫을 뺀 것), 그리고 학습 쪽으로는 원본들 사이의 전략 일관성. **← 품질 계열 원인** (어떤 데모가 재료인가가 결정).
- **생성 실패 = 가장 빡빡한 접촉 깔때기에서 부하가 예산을 처음 초과하는 사건.** 원본 쏠림 = 예산의 원본 간 이질성. 절멸 = 변위 0에서도 예산이 잡음 바닥 아래인 원본.
- **정책 성능은 한 층 위의 병목 지도가 결정한다.** 사전 계산 가능한 자원 4개 — 공차-스케일 반경에서의 생존 커버리지, 감독 일관성, 전략 다중성, 생존자 마진 — 중 그 작업에서 무엇이 병목인가. 개입이 병목이 아닌 여유 자원을 건드리면 무효가 된다(우리의 null들이 그 사례).

## 2. 생성기 코드에서 유도된 구조 정리 두 개 (적합이 아니라 유도)

**정리 1 — 평행이동 등가성과 누출 4경로.** 참조 물체를 순수 평행이동하면 구간 내부의 행동열은 원본과 비트 단위로 동일하다(물체 좌표계 강체 변환의 성질). 따라서 평행이동이 만드는 실패는 오직 네 경로로만 들어온다: ① 구간 사이 다리(보간), ② 팔 관절 재배치, ③ 자세 스냅숏의 노후화(받는 물체 자세를 에피소드 시작에 한 번만 읽음), ④ 행동 잡음. — 평행이동이 상대적으로 순한 이유와, 아플 때 정확히 어디가 아픈지가 이걸로 특정된다.

**정리 2 — 다리의 상한.** 구간을 잇는 보간은 **5스텝 고정**으로 하드코딩돼 있고 스텝당 이동 한계는 0.05m다. 즉 다리가 감당할 수 있는 이동은 최대 0.25m — 그 이상 벌어진 접합부에서는 실패가 **선형이 아니라 문턱형**으로 나타난다. 이것이 이 프레임워크의 가장 기계적인 예측이며, 검증 비용이 가장 싸다(config 한 줄 변경).

## 3. 요인 목록 (12개, 수준 × 다양성/품질)

| 수준 | 요인 | 다양성/품질 | 한 줄 정의 |
|---|---|---|---|
| 데모 | 속도 여유(템포 마진) | 품질 | 스텝당 0.05m 한계 대비 원본 이동량의 여유 합. 빠르고 손목 위주로 급하게 움직인 데모는 변형이 만든 지연을 흡수할 자본이 없다 → 원본별 생존 곡선의 **절편**을 결정 |
| 데모 | 접촉 깔때기 잔여 예산 | 품질 | 작업 공차 용량 − 데모 실행이 이미 소비한 접촉 배치 편심. 데모마다 같은 깔때기를 다른 비율로 소진 |
| 데모 | 전략 다중성·감독 일관성 | 품질 | 물체 기준 좌표계에서 원본들의 행동 불일치(H(A\|S))와 전략 군집 간 비호환 정도. **원본별 균등 뽑기가 해로운 메커니즘의 자리**이자 독성 고DGR 원본의 자리 |
| 데모×크기 | 한계 생존자 질량 μ | 품질 | 예산을 거의 소진하고 살아남은 아슬아슬한 생존 데모의 비중. DGR이 못 보는 생존자 품질 채널 |
| 방법 | 평행이동 등가성·누출 구조 | 양쪽 | 정리 1. 실패 진입 경로를 4개로 한정 |
| 방법 | 고정 5스텝 다리 | 양쪽 | 정리 2. 접합부 길이에 문턱형 실패 + 시연된 적 없는 모션 구간 생성 |
| 방법 | 자세 스냅숏 노후화 | 품질 | 받는 물체 자세를 시작 시 1회만 읽음 → 도중에 물체가 밀리면 배치 구간이 낡은 자세를 참조 |
| 방법 | 성공-필터 = 이방성 투영기 | 다양성 | 필터는 커버리지를 균일하게 깎지 않고 부하가 큰 축(받는 물체·구석·회전)을 선택적으로 깎는다 |
| 방법 | 행동 잡음 = 강건성 검증기 | 양쪽 | 잡음 제거는 DGR을 올리며 정책을 망친다(논문 App N) — 잡음이 한계 생존자를 걸러주는 검증 역할 |
| 크기 | 물체별·대칭 가중 성분 부하 벡터 | 다양성 | 집는/받는 물체 이동, 지렛대 증폭 회전, 접합부 길이의 성분별 벡터. 스칼라 거리는 이것의 손실 있는 투영 |
| 크기 | 절대 작업공간 지리·여유 공간 | 다양성 | 같은 상대 변위라도 작업공간 가장자리·장애물 근처에서는 부하가 다르다 |
| 크기 | 작업 깔때기 용량 C_s | 다양성 | 공차 스케일. **커버리지 필요 반경이 ρ=αC_s로 공차에 비례** — 커피(mm급 공차)는 far가 필요하고 블록(널널)은 불필요한 이유 |

## 4. 관측 설명표 (전 관측 커버)

| 관측 | 설명 경로 |
|---|---|
| 거리↑→생성 성공률↓ (9/9) | 모든 부하 성분이 영역 확장에 단조 증가, 고정 예산과 충돌 |
| 원본 쏠림·절멸 | 원본별 예산 이질성의 곱셈 구조(구간별 생존율의 곱) → 두꺼운 꼬리; 절멸 = 변위 0에서 예산 < 잡음 바닥 |
| 받는-물체 이동의 선택적 도태 | ① 쥐는 동작이 집는 쪽 오차를 리셋(깔때기 수렴)하는 반면 받는 쪽 오차는 배치까지 생존 ② 스냅숏 노후화가 받는 쪽에만 작용 ③ 실패가 후반(배치 단계)에 몰림(실측 0.78~1.00) |
| 품질 인과 +7~22%p | 원본 교체는 품질 셀(생존자 마진 μ + 감독 일관성)만 움직임 — 합성 데모는 원본의 스타일 클론이므로 |
| DGR의 불완전성 | DGR은 생존 법칙의 적분일 뿐, 생존자의 학습 적합성(μ, H(A\|S))에 눈멂. 논문의 잡음 제거 실험(DGR +33, 정책 −30~40)이 같은 구조 |
| 구석 절벽 | 물체별 **결합** 커버리지 기준으로 구석은 질량이 거의 0 — 스칼라 거리 커버리지가 멀쩡해 보여도 |
| 커버리지 필요의 작업 의존 | ρ=αC_s: 공차가 빡빡한 작업일수록 커버리지 반경이 좁아 far 상실이 병목이 됨 |
| 회전의 추가 난이도 | 지렛대 숨은 이동 2r·sin(Δψ/2) + 손목 한계 + 접근 방향 변화 — 평행이동에는 없는 세 가산 벌점 |
| 재균등화 null | 병목 논리: 이미 커버된 support 안의 밀도 재배분은 여유 셀 개입 |
| 원본 균등화의 stack 한정 해악 | 비호환 소수 전략 군집의 질량 재주입 → H(A\|S) 상승; 작업 선택성 = 군집 간 비호환 격차의 작업 차이 |
| 밀도 방향의 작업별 분열 | 부호 규칙: 근접-껍질 감독 불일치가 크면 near-heavy 유리, 공차-스케일 커버리지가 병목이면 far-heavy 유리 (사후 설명 — 사전 검증 예정) |
| 풀 내부 거리-성공의 잡음성 | 같은 스칼라 거리가 전혀 다른 성분 조합을 뭉뚱그림 — 투영 인공물로 해소 |

**설명 못 하는 것(정직 목록):** 학습기 용량·아키텍처 효과(BC-RNN 36% vs Diffusion 76%는 데이터가 아니라 학습기 문제), 이미지 관측 전이, 목표 분포 정렬 효과, 실수-복구 상태 커버리지, 접촉 힘 마진(데이터에 힘 채널이 없어 프록시로 때우지 않고 배제), 소표본 쏠림 인플레이션(보정 후 값을 예측 대상으로 삼아야 함).

## 5. 조사 중 실측으로 확정된 것

- 원본의 속도-여유/스타일 합성 지표가 원본별 DGR 순위를 예측(Spearman 0.43~0.51; 단 결과를 보고 고른 위험이 있어 **미사용 회전 풀에서의 held-out 검증이 결정 시험**).
- 소박한 여유 지표들(쥐기 중심 오프셋·저크·멈춤 비율)은 **예측 실패**(p>0.35) — 이전에 내가 제안했던 순진한 지표들이 죽었다는 정직한 null.
- 받는-물체 이동 시도의 실패는 후반부(배치 단계)에 몰림(실패의 0.78~1.00) — 메커니즘 ③의 직접 증거.

## 6. 검증 계획 (비용 순, 각각 반증 조건 포함)

1. **접합부 부하 검정** (수 시간, 새 실행 0): 기존 로그의 모든 시도에 대해 접합부 길이 L_j를 자세 산술로 계산, 같은 장면 거리에서 L_j가 성공을 d_pos보다 잘 예측하는지 로지스틱 비교.
2. **P-A1 절멸·순위 사전등록**: 여유/예산 추출기를 지금 동결하고, 9작업×10원본의 예측(원본별 DGR 순위, 절멸 집합)을 먼저 발표한 뒤 미사용 회전 풀의 실측을 연다. 실패 조건: 절멸 원본이 예산 중위권.
3. **P-C1 동결-전이 성분 법칙**: 부하 벡터→성공의 링크 함수를 블록 쌓기에서만 적합해 동결, 나머지 8작업을 예측. 적합 스칼라 개수를 공개.
4. **P-C2 다리 개입** (가장 싼 결정 시험): 접합부 길이에 비례해 보간 스텝을 늘리는 config 한 줄 변경으로 far 구간 DGR 회복량을 **사전에 %p로 예측**하고 재생성해 대조.
5. **P-B1/B2 정책 쪽 사전등록**: 전략 비호환 지수(G)로 망치·머그의 원본 균등화 해악 여부, stack_three·망치의 밀도 방향 부호를 **학습 전에** 공표하고 실행.
6. **P-A2 μ 독성** (유일하게 새 학습 필요): 보정에 안 쓴 작업에서 DGR은 같고 μ만 다른 원본 쌍으로 풀을 만들어 학습 대조. 맨 마지막.

## 7. 개선 지렛대 (요인별 처방)

| 지렛대 | 겨냥 요인 |
|---|---|
| 접합부 길이 비례 보간 스텝 (+필요시 dwell) | 고정 5스텝 다리 |
| 성분 부하/예산 비율 최소화 원본 선택 (스칼라 최근접의 일반화) | 부하 벡터 × 잔여 예산 |
| 생성 전 원본 큐레이션: 여유 하위 10% 배제, 비호환 전략 군집은 균등화 대신 정리 | 속도 여유 + 전략 다중성 |
| 생존-커버리지 겨냥 시도 배분: 필터가 깎을 곳(받는-물체 축·구석)을 미리 과표집해 **필터 후** 분포를 목표에 맞춤 | 이방성 투영기 + C_s |
| 배치 구간 시작 시 받는 물체 자세 재판독 | 스냅숏 노후화 |
| 마진 인지 필터링: 성공했어도 예측 잔여 마진이 바닥이면 배제, 잡음은 유지 | μ + 잡음 검증기 |

## 8. 신규성의 위치 (red-team 판정)

다양성/품질 축 자체는 Belkhale et al.(NeurIPS 2023)의 것이고, MimicGen 부록들에 관측 다수가 이미 있다. 새로운 것은 ① **사전 계산 가능성이라는 메타 수**: 계획 궤적이 닫힌형이므로 시도 풀 전체를 생성 전에 감사할 수 있고, 생성·학습 로그는 검증 대상으로 강등된다 — 기존 연구는 이런 양을 생성 내부의 선택기로만 썼지 사전 감사 도구로 쓴 적이 없다; ② 생성기 코드에서 유도한 구조 정리 2개(평행이동 등가성·누출 4경로, 5스텝 다리 상한); ③ 정책 쪽 병목-셀 층(개입 무효/유효의 예측 구조).
