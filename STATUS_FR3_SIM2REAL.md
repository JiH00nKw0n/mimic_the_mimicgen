# FR3 sim2real 협업 — 작업 현황판

갱신 2026-08-05(3차). 번호는 재익님이 정리해 준 우선순위 목록 그대로다. 각 항목에 "무엇을
하는 일인지 → 지금 상태 → 근거 수치 → 산출물 위치 → 남은 것"을 적는다. 맥락 없는 독자용 통합 보고서는
`contract/FINAL_REPORT.md`이고, 세부 과정은 `contract/PROGRESS.md`(계약·비교 심층 기록)와
`Stage2_Physics_DGR_Ablation.md`(물리 ablation 리포트)에 있다.

## 1. MimicGen을 control-contract에 맞추기 — 사실상 완료 (회신 대기 1건)

우리 생성 데이터의 행동을 RL팀 실기 제어 규격(10 Hz, 7숫자 상대명령, 동결 OSC)으로
번역하는 후처리 파이프라인을 만들고 검증하는 일.

- 변환 파이프라인 완성: 시연 → 계약 형식 HDF5. 검증 3종 통과 — 자세↔행동 왕복 오차
  ~1e-18(수식 정확), 월드 TCP 복원 평균 1.0 cm(좌표 규약 정확), 계약 코드가 RL팀
  패키지본과 바이트 동일(드리프트 없음).
- 계약 제어기 재실행(warm-start): RL 런타임 설정(중력 off, 솔버 36, 리밋, Isaac 5.1
  자산) 그대로 적용해 측정 완료. 잔차 = 상수 바닥 ~3.4 cm + 속도 비례 성분(사람 원속
  7.5 / 감속 5.4 / RL 데모 10.9 cm). 이 제어기에서 녹화된 RL 데모조차 개루프 복사가 안
  되므로, "복사 후 태스크 성공"은 설계상 불가능한 기준 — 폐루프 정책이 흡수하는
  성분이라는 결론.
- RL팀 소스 규약 질문지 답변 완료: `contract/mimicgen_inputs_supplied.yaml`.
- **남은 것**: `contract/REPORT_TO_RL_20260803.md`를 재익님께 전달하고 상수 바닥의
  자기들 기준선·수용 기준 회신을 받는 것(전달은 사용자 액션). 선택: 새로 받은
  `fr3_research3_massfix.usda`(질량 보정판)로 재실행해 상수 바닥이 줄어드는지 확인.

## 2. 지환님 range로 데모 수율 테스트 — 완료

stage2 접촉 캘리브레이션 범위(물리 파라미터)를 흔들어도 MimicGen 생성이 잘 되는지
robosuite 파이프라인에서 확인하는 일.

- stack·square × 물리 5개 조건 + hi-DGR 소스 선별 ablation 완료(조건당 수백 시도).
- 결과: stack은 물리 변화에 둔감, square는 파지 마찰에 민감. 소스별 수율 랭킹은 물리를
  바꿔도 유지(ρ 0.92~0.99) → hi-DGR 소스 선별 전략이 새 물리에서도 유효(수율 1.6~2.6배).
- 시각 랜덤화는 저차원 생성 수율에 구조적으로 영향 불가라 렌더 검증으로 수행(mp4 확인).
- 산출물: `Stage2_Physics_DGR_Ablation.md` + `stage2_figs/`, 레코드는 로컬
  `robot_data/stage2_ablation_data/`.

## 3. 최종본 + 카메라로 RGB-Action 증강 — 파이프라인 완료, 시험 데이터셋 생성 (결함 2건 해결)

생성한 시연에 실기 카메라와 같은 시점의 렌더 영상을 붙여 (영상, 계약 행동) 학습
데이터를 만드는 일.

- **시험 데이터셋**: 서버 `contract_out/rgb_action_final10.hdf5` — 10 에피소드 /
  1,760 스텝, 카메라 3대(third_person_0·1, wrist) 180×320, **2026-07-28 실측
  캘리브레이션**, 실험실 책상 에셋, 50/40/10 시각 랜덤화, 계약 스키마 PASS,
  이미지 대비 47.8~49.7(게이트 10.0).
- **좌표계 일관성 검증**: 큐브가 받침 +x, 고정 카메라 2대 모두 10/10 에피소드에서
  큐브 3개가 화면 안(교정 전 0/9).
- **해결한 결함 2건**: ① 카메라가 스폰 자세를 따라가 로봇과 180° 어긋나 바닥을 비추던
  문제(조준 오차 0.99→0.15 m), ② 생성 데이터의 받침 좌표계가 실기와 180° 반대였던 문제
  (원인: 설정 회전값의 의미가 Isaac Lab 버전 변경으로 뒤집힘). ②는 수율까지 1.9배
  올렸다(6.5→12.4%).
- **남은 것**: 학습용 규모로 확대(현재 10 에피소드 시험 규모). 커튼·벽은 RL팀 규격이
  랜덤화 대상으로 지정하지만 패키지가 색·재질 값만 주고 3D 에셋이 없어 적용 불가 —
  코드가 `skipped`로 기록하며, 필요하면 누가 에셋을 만들지 정해야 한다.
- 상세: `render/RGB_ACTION_PIPELINE.md` (GIF·프로파일 비교 이미지 포함).

## 4. 사람 vs RL-teacher 수율 비교 — 완료 (3-cube로 스코프 확정, peg 제외 결정)

받침 좌표계 교정 후 재측정한 값이 유효하다.

| 조건 | 소스 | 수율 | 95% CI |
|---|---|---|---|
| **교정 후** | 사람 (13개) | **25/201 = 12.4%** | 8.6~17.7 |
| **교정 후** | RL 재작성 + 2배 감속 (50개) | **25/345 = 7.2%** | 5.0~10.5 |
| 교정 전 | 사람 | 25/385 = 6.5% | 4.4~9.4 |
| 교정 전 | RL 원본 스타일 | 0/3117 = 0% | — |
| 교정 전 | RL 재작성 + 2배 감속 | 25/486 = 5.1% | 3.5~7.5 |

- 결론: RL 시연은 원본 그대로는 증폭 불가(0%), 사람 스타일 재작성이면 7.2%까지 되지만
  **사람 시연이 여전히 1.7배 높다**(12.4% vs 7.2%, z=2.03, p=0.043). 교정 전 데이터로는
  두 값이 구분되지 않아 "RL이 사람 수준"으로 보였는데, 좌표계 교정 후 결론이 뒤집혔다.
  RL 0%의 원인은 6종 결함 사슬로 전부 규명. 성공의 소스 편중은 사람·RL 공통 특성.
- 도구: `contract/resignal_rl.py`(소스 재작성), `contract/densify_source.py`(감속).
- 데이터(교정 후): 서버 `contract_out/gen_flip_25.hdf5`(사람), `gen_rl_flip_25.hdf5`(RL) + provenance.

## 4-b. (8/3 슬랙에서 추가된 약속) 합성 데이터로 정책 학습 비교 — 미착수

수율 비교의 다음 단계로 지훈님이 재익님께 약속한 항목: RL-teacher 데모(+재작성 파이프
라인)로 합성한 데이터로 정책(BC)을 학습해, 기존 "사람 데모 + 원래 컨트롤러" 합성 데이터로
학습한 정책과 성능을 비교한다. 근거 맥락: 재익님 관측으로는 RL-teacher 정책 자체는 물리
보정(SysID) 시뮬에서 성능이 크게 떨어지는 반면 MimicGen(사람 데모) BC는 유지됨 — RL
데모 유래 합성 데이터가 어느 쪽을 닮는지가 질문. 학습 데이터 규모를 위해 성공 데모를
수백 개 단위로 추가 생성해야 하며(현재 RL 재작성 성공 35개), 이는 3번(RGB) 이전의
상태 기반 BC로 먼저 할 수 있다.

실험은 두 개로 쪼개는 편이 낫다. 슬랙에서 이야기된 형태("RL 데모 + 새 컨트롤러" 대
"사람 데모 + 원래 컨트롤러")는 소스와 제어기를 동시에 바꿔 원인 귀속이 안 되기 때문이다.
(A) 제어기를 생성용 IK-rel로 고정하고 소스만 사람/RL로 바꿔 비교 — "RL 원료가 쓸 만한가"에
직접 답한다. (B) 소스를 사람으로 고정하고 행동 표현만 바꿔(IK-rel 학습 대 계약 형식 학습 +
계약 제어기 평가) 비교 — 제어기 전이 비용을 격리해 재며, 1번의 마지막 미검증 고리다.
선행 조건: 소스별 성공 수백 개 규모 생성(사람 12.4% 기준 성공 200개에 ~1,600 시도).

## 5. multi-gear — 보류 (재익님: 급하지 않음, 3번째 태스크)

## 6. sim2real 요인 ablation — 보류 (논문화 단계에서 재익님이 재공지 예정)
