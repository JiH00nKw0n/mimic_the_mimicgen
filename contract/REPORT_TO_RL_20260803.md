# 재익님께 — control-contract 정합 검증 결과 보고 (2026-08-03)

보내주신 `fr3_mimicgen_control_handoff_20260803` 잘 받았습니다. 무결성 검증(체크섬
21/21, verify_package.py 4항목 PASS) 후, 패키지의 런타임 설정(로봇 중력 비활성, 솔버
36/0, effort 87/12 Nm, 속도 2.175/2.61 rad/s, 그리퍼 1e4/5e2/500, fr3_research3.usda
래퍼 자산)을 저희 재실행 환경에 그대로 적용해 다시 검증했습니다.

## 확인된 것 (정합 OK)

- 컨트롤러 코드(actions_cfg, task_space_actions)와 계약/스키마 yaml은 저희가 쓰던
  vendored본과 **바이트 단위로 동일**합니다. 드리프트 없습니다.
- 저희 변환 파이프라인의 액션 언어 검증: pose→action→pose 왕복 오차 ~1e-18, 기록
  관측 대비 월드 TCP 복원 평균 1.0 cm, 스키마 검증 PASS.
- `mimicgen_inputs_required.yaml`에 대한 답변을 `mimicgen_inputs_supplied.yaml`로
  작성해 두었습니다 (소스 프레임/쿼터니언 순서/샘플레이트/그리퍼 규약/큐브 매핑/서브
  태스크 규칙 전부).

## 보고 드릴 이상치 (RL 오너 판단 요청)

사람 demo_0을 계약 형식으로 변환한 뒤, 동결 OSC로 시뮬에서 재실행(개루프: 기록된
델타 액션을 현재 actual 기준으로 순차 적용)했을 때의 추적 잔차입니다:

| 소스 · 재생 속도 | 평균 잔차 | p95 | 쌓기 성공 |
|---|---|---|---|
| 사람 demo, 2배 감속 (348스텝) | 5.4 cm | 9.8 cm | 실패 |
| 사람 demo, 원속 (174스텝@10Hz) | 7.5 cm | 13.3 cm | 실패 |
| **RL demo (59스텝@10Hz, 가장 빠름)** | 10.9 cm | 21.6 cm | 실패 |

세 점이 잔차 = **속도 비례 성분 + 상수 성분(~3.4 cm)** 모델에 일관되게 맞습니다.
특히 RL 시연 — 바로 이 동결 제어기 위에서 폐루프로 녹화된 데이터 — 조차 개루프
재생으로는 잔차가 가장 크게 나옵니다(속도가 가장 빠르므로). 즉 "기록 궤적의 개루프
복사"는 이 제어기의 설계상 달성 불가능한 기준으로 보입니다. 속도 비례 성분은 과감쇠 게인(τ≈0.42 s)에서 사전 기록
델타를 재생할 때의 필연적 지연(폐루프 정책은 스스로 보정하는 성분)으로 이해하고
있습니다. 질문은 상수 성분입니다:

1. RL팀 내부에서 기록된 액션 시퀀스를 재생하는 테스트를 하실 때 잔차가 어느 정도
   나오는지요? (저희 ~3.4 cm 상수 바닥이 재현되는지)
2. 계약 정합의 수용 기준이 "재실행 태스크 성공"인지, 아니면 adapter 왕복 검증
   수준인지 확인 부탁드립니다. 게인·자산이 동결이라 저희 쪽에서 상수 성분을 더
   좁히기 어렵습니다.

세부 수치와 재현 커맨드는 저장소 `contract/PROGRESS.md`에 있습니다.
