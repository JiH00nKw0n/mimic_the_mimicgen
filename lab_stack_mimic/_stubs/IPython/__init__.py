# Minimal IPython stub so isaaclab_mimic.datagen.utils (which does
# `from IPython.display import display`) imports under the headless env_uwlab venv
# WITHOUT installing IPython into the colleague's shared environment.
